import { GISLayer, FieldReport } from '../types';

// Zero mock layers — real GIS datasets load dynamically from Google Drive folders & user imports
export const INITIAL_GIS_LAYERS: GISLayer[] = [];

export const INITIAL_FIELD_REPORTS: FieldReport[] = [];
